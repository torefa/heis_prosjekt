//Header for the timer class
//
#include <stdio.h>
#include <unistd.h>

// Function to start the timer.
int timer_start(){
  sleep(3);
  return 0;
}

// Funtion to notify that the time is out
int time_out(){
 
}
